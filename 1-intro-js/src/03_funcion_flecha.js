const obtenerInfo = (name = "César", apellido = "Guerra") => `${name} ${apellido}`;
const sum = (a = 0, b = 0) => a + b;

const info = obtenerInfo("Alex", "González");

console.log(info)
console.log(20, 10);
